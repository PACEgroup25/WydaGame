import {type Entity, type EntityProfile, type EntityRole, type EntityHome} from "./entity.ts";
import { populateEntity } from "./populateEntity.ts";

export async function buildClient(role:string){
    if(role == 'Learning Coach'){

    } else if (role == 'Learner'){

    } else if (role == 'Client Admin'){
        
    }
    
}

// export async function createProfile(){
    
// }